{
  "name": "viper-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Viper common auth + permissions service (multi-tenant, per-project RBAC). Dev AuthEngine = JWT + console OTP; swappable for Insforge/Keycloak in prod.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "smoke": "node smoke.cjs"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "fastify": "^4.29.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^9.0.3"
  }
}
