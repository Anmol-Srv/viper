# Non-secret — also written into viper.json by the portal at zip time
AUTH_SERVICE_URL=http://localhost:4000
PROJECT_ID=prj_dev
AUTH_CLIENT_ID=
VIPER_URL=http://localhost:3300

# Secret — server only. Coolify env in prod, this file locally. Never commit.
AUTH_CLIENT_SECRET=
VIPER_DEPLOY_TOKEN=

# db module only
INSFORGE_URL=
INSFORGE_API_KEY=

# Local dev: skip the login wall, act as dev@airtribe.live (role owner, all permissions)
AUTH_DEV_BYPASS=1
